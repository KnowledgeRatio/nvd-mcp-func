# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: nvd-mcp
hooks:
  preprovision:
    windows:
      shell: pwsh
      run: scripts/create-app-registration.ps1
    posix:
      shell: sh
      run: scripts/create-app-registration.sh
services:
  api:
    project: ./src/
    language: python
    host: function
