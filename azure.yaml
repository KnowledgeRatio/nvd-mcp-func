# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: nvd-mcp
hooks:
  preprovision:
    - shell: pwsh
      run: scripts/create-app-registration.ps1
services:
  api:
    project: ./src/
    language: python
    host: function
